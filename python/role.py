class Role(object):
    def inviter(personne):
        raise NotImplementedError("You must implement foo's %s method" % type(self).__name__)
    def manger(personne):
        raise NotImplementedError("You must implement foo's %s method" % type(self).__name__)
